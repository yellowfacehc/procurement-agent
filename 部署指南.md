# 采购意向快速获取工具 - Render部署指南

## 一、准备工作

### 1. 注册账号
- GitHub: https://github.com
- Render: https://dashboard.render.com （可以用GitHub账号直接登录）

### 2. 安装Git
- 下载: https://git-scm.com/download/win
- 安装时全部默认即可

---

## 二、创建GitHub仓库

### 方法一：网页操作（推荐新手）

1. 打开 https://github.com/new
2. Repository name 填：`procurement-agent`
3. 选择 Public（公开，免费）
4. 不要勾选 "Add a README file"
5. 点击 "Create repository"

### 方法二：命令行操作

打开PowerShell，执行：

```powershell
# 进入项目目录
cd C:\Users\14034\DoubaoWork\chats\2026-08-28\new-chat

# 初始化Git
git init
git add .
git commit -m "初始版本"

# 关联GitHub仓库（把下面的用户名改成你的GitHub用户名）
git branch -M main
git remote add origin https://github.com/你的用户名/procurement-agent.git
git push -u origin main
```

---

## 三、在Render上部署

1. 打开 https://dashboard.render.com
2. 点击右上角 "New +" → "Web Service"
3. 选择 "Build and deploy from a Git repository"
4. 点击 "Connect account" 连接你的GitHub账号
5. 找到 `procurement-agent` 仓库，点击 "Connect"

### 配置参数（重要！）

| 配置项 | 值 |
|--------|-----|
| Name | procurement-agent（随便起） |
| Region | Singapore（新加坡，离中国近） |
| Branch | main |
| Runtime | Python 3 |
| Build Command | `pip install -r requirements.txt` |
| Start Command | `gunicorn wsgi:app --bind 0.0.0.0:$PORT` |
| Instance Type | Free（免费） |

5. 点击 "Create Web Service"
6. 等待部署完成（约2-3分钟）

---

## 四、访问应用

部署成功后，Render会给你一个网址，类似：
```
https://procurement-agent.onrender.com
```

任何人都可以通过这个网址访问，不需要在同一个局域网。

---

## 五、注意事项

### 1. 免费版限制
- 应用15分钟没有访问会自动休眠
- 下次访问需要等待约30秒启动
- 每月有750小时免费额度（足够用）

### 2. 爬虫IP问题
- Render的服务器在国外，爬取重庆政府采购网可能会慢一些
- 如果遇到访问失败，可以等一会儿重试

### 3. 更新代码
- 修改本地代码后，执行 `git add . && git commit -m "更新" && git push`
- Render会自动重新部署

---

## 六、文件说明

| 文件 | 作用 |
|------|------|
| procurement_agent.py | 主程序 |
| wsgi.py | WSGI入口（生产环境用） |
| requirements.txt | Python依赖 |
| render.yaml | Render配置文件 |
| 使用说明.md | 本地使用说明 |
| 部署指南.md | 本文件 |
